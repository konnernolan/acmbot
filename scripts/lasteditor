# Displays last editor
# If you are reading this, this file has not been tested

lasteditor = (x) ->
	x + "is the last editor"
console.log lasteditor("x") # Replace x or old name with your name, email, etc

module.exports = (robot) ->
	robot.respond /lasteditor/ ->
	x + "is the last editor"
	
